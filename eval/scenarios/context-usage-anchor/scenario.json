{
  "formatVersion": 1,
  "schema": "och.eval.scenario",
  "id": "context-usage-anchor",
  "description": "Real history stays below the trigger on its own wire estimate, then one fixture response reports a deliberately high provider input-token count. Passing requires the next preparation to record usageAnchorApplied with a non-lowering value supported by that earlier provider usage record, and a pre-turn compaction to actually follow. A high fixture number or enabled configuration alone cannot satisfy this: the applied anchor must be in the evidence.",
  "fixtureDigest": "sha256:f5043b2c09fb8ecfc394548860794027a0f22602668cee36597f41b9f349cefd",
  "fixtureCopyPolicy": {
    "maxFiles": 10,
    "maxFileBytes": 1048576,
    "maxTotalBytes": 1048576
  },
  "actions": [
    {
      "id": "prompt-1",
      "type": "prompt",
      "prompt": {
        "text": "Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. "
      }
    },
    {
      "id": "prompt-2",
      "type": "prompt",
      "prompt": {
        "text": "Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. "
      }
    },
    {
      "id": "prompt-3",
      "type": "prompt",
      "prompt": {
        "text": "Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. "
      }
    },
    {
      "id": "prompt-4",
      "type": "prompt",
      "prompt": {
        "text": "Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. "
      }
    },
    {
      "id": "prompt-5",
      "type": "prompt",
      "prompt": {
        "text": "Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. "
      }
    },
    {
      "id": "prompt-6",
      "type": "prompt",
      "prompt": {
        "text": "Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. "
      }
    },
    {
      "id": "prompt-7",
      "type": "prompt",
      "prompt": {
        "text": "Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. "
      }
    },
    {
      "id": "prompt-8",
      "type": "prompt",
      "prompt": {
        "text": "Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. Real conversation history large enough that summarizing it is a genuine reduction, while the plain wire estimate for the next request still stays below the trigger so only the provider usage anchor can cross it. "
      }
    },
    {
      "id": "prompt-anchor",
      "type": "prompt",
      "prompt": {
        "text": "Report usage for this turn. OCH_EVAL_CONTEXT_USAGE_ANCHOR"
      }
    },
    {
      "id": "prompt-after",
      "type": "prompt",
      "prompt": {
        "text": "Continue briefly."
      }
    }
  ],
  "requiredCapabilities": [
    "prompt"
  ],
  "requiredEvidenceRoles": [
    "transcript",
    "audit"
  ],
  "deterministicVerifierIds": [
    "context-usage-anchor-v1",
    "context-budget-bounds-v1",
    "context-projection-present-v1",
    "outcome-not-infra-failed-v1"
  ],
  "limits": {},
  "pairingTags": [
    "context-anchor"
  ]
}
