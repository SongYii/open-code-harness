{
  "formatVersion": 1,
  "schema": "och.eval.scenario",
  "id": "executor-parity",
  "description": "An approved write_file tool call, run through both the in_process and acp_subprocess executors against the same deterministic fixture (design section 25.1's own executor parity fixture). Baseline and candidate must yield the same declared semantic facts -- terminal state, tool/usage/request facts, and workspace result -- even though every lifecycle fact (process, lease, IDs, timestamps) legitimately differs between the two executors.",
  "fixtureDigest": "sha256:f5043b2c09fb8ecfc394548860794027a0f22602668cee36597f41b9f349cefd",
  "fixtureCopyPolicy": {
    "maxFiles": 10,
    "maxFileBytes": 1048576,
    "maxTotalBytes": 1048576
  },
  "actions": [
    {
      "id": "prompt-1",
      "type": "prompt",
      "prompt": {
        "text": "please write to the workspace file TRIGGER_WRITE_FILE_APPROVAL"
      }
    },
    {
      "id": "collect-1",
      "type": "collect",
      "collect": {
        "workspacePath": "denied.txt"
      }
    }
  ],
  "approvalScript": [
    {
      "promptActionId": "prompt-1",
      "ordinal": 0,
      "toolName": "write_file",
      "answer": "allow"
    }
  ],
  "requiredCapabilities": ["prompt"],
  "requiredEvidenceRoles": ["transcript", "audit", "workspace"],
  "deterministicVerifierIds": [
    "outcome-not-infra-failed-v1"
  ],
  "limits": {},
  "pairingTags": ["executor-parity"]
}
